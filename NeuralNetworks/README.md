# NeuralNetworks

A simple Python library to train and evaluate feedforward neural networks on MNIST.

## Features

- Fully connected neural networks with multiple layers
- Supports multiple training runs
- Track and plot loss and accuracy over epochs
- Save and load models easily

## Installation

Install via pip:

```bash
pip install NeuralNetworks