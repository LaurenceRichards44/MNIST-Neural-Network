[project]
name = "NeuralNetworks"
version = "0.1.0"

dependencies = [
    "numpy",
    "scikit-learn",
    "matplotlib"
]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["NeuralNetworks"]